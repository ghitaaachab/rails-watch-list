<p><%= @list.name %></p>

<hr>

<%= form_for @list do |f| %>
  <div class="field">
    <%= f.label :movie_id %>
    <%= f.collection_select(:movie_ids, Movie.all, :id, :title, prompt: "Select a Movie") %>
  </div>

  <%= f.submit "Create Bookmark" %>
<% end %>
